machine:
 xcode:
 version: “9.1”
dependencies:
   override:
     - pod install:
jobs:
  build:
    working_directory: ~/AnkitInterview
    branches:
        only:
        - master

    docker:
      - image: circleci/ios:api-26-alpha
