machine:
 xcode:
 version: “9.1”
dependencies:
   override:
     - pod install:
jobs:
  build:
    working_directory: ~/AnkitInterview
    branches:
        only:
        - master

    docker:
      - image: circleci/ios:api-26-alpha

steps:
      - checkout

      - store_artifacts:
          path: /Users/distiller/AnkitInterview/AnkitInterview.ipa
          destination: ipa/
